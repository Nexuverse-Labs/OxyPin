# Xposed modules rely on reflection and entry points listed in assets/xposed_init,
# so keep minification disabled (isMinifyEnabled = false).
# If you ever enable it, add keep rules for ru.oxypin.** here.
